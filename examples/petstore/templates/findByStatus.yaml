adapter: findByStatus
protocol: http

request:
  method: GET
  path: /pet/findByStatus?status={{status}}
  headers:
    Accept: application/json

response:
  extract:
    pets:
      path: "$"
      fields:
        petId: "id"
        petName: "name"
        petStatus: "status"

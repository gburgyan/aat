# Built binaries
/aat
/aat-sandbox
/aat-gateway
*.exe

# IDE
.idea/
.vscode/
.run/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Run output (generated)
_output/
runs/
traces/

# SQLite
*.db
*.db-shm
*.db-wal

# Frontend: Vite builds into dist/app; dist/placeholder.txt is tracked
server/web/node_modules/
server/web/dist/app/

# Docs site (MkDocs build output and its virtualenv)
site/
/.venv-docs/

# Python
__pycache__/
/venv/

# Secrets
.env
.env.*

# Local per-developer config (the repo-root MCP config only; examples ship theirs)
.aat-overrides.yaml
/.mcp.json

# Environment configs (may contain credentials)
environments/
/setup-local.sh

# Scratch files at the repo root
/graph.yaml
/package-lock.json

# make demos: pinned tools and the assets that are not committed
/demos/node_modules/
/demos/.bin/
/demos/out/

# goreleaser output
/dist/

# Local planning notes
/LAUNCH-PLAN.md

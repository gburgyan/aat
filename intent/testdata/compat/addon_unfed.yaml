metadata:
  prompt: "addon with an unfed AUTOWIRE input"

execution:
  steps:
    - node: addonNode
      description: "Addon that needs workbenchId and specialInput"
      values:
        workbenchId: AUTOWIRE
        specialInput: AUTOWIRE

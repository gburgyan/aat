execution:
  steps:
    - node: createCart

    - node: checkout
      dependsOn: [createCart]
      values:
        cartId: AUTOWIRE
        giftMessage: AUTOWIRE?

    - node: confirmOrder
      dependsOn: [checkout]
      values:
        orderId: AUTOWIRE

version: "1.0.0"

nodes:
  stepA:
    description: "Step A"
    adapter: stepA
    inputs:
      - name: input1
        type: string
    outputs:
      - name: output1
        type: string

  stepB:
    description: "Step B"
    adapter: stepB
    inputs:
      - name: input1
        type: string
    outputs:
      - name: output1
        type: string

edges:
  - from: stepA.output1
    to: stepB.input1
    select: true
